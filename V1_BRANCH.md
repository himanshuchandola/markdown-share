# v1 branch (legacy)

The **v1** app (Next.js Pages + MongoDB) is on a separate branch.

- **Branch:** [`v1`](https://github.com/himanshuchandola/markdown-share/tree/v1)
- **To use it:** `git checkout v1` then `pnpm install && pnpm dev`

This repo’s default branch (`main`) is the v2 app (Next.js App Router + Supabase).
